-- script.lua  –  optional user script
print("Lua script loaded!")

-- You could define your own hello_world here, but the C side already
-- provides one, so we just call it from the button.

hello_world()